<UserControl
    x:Class="PyStudentIDE.UI.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel Width="300" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Iniciar Sesión" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" TextAlignment="Center"/>
            <TextBlock Text="Correo institucional" Margin="0,5"/>
            <TextBox x:Name="EmailBox" Height="30" Margin="0,5"/>
            <TextBlock Text="Contraseña" Margin="0,5"/>
            <PasswordBox x:Name="PasswordBox" Height="30" Margin="0,5"/>
            <Button Content="Ingresar" Height="40" Margin="0,20" Click="OnLoginClick"/>
            <TextBlock x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</UserControl>
