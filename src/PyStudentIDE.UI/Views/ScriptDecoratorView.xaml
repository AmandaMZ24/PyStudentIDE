<UserControl
    x:Class="PyStudentIDE.UI.Views.ScriptDecoratorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{DynamicResource WindowBackground}"
    Foreground="{DynamicResource PrimaryText}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <Button Content="📂 Cargar script" Style="{DynamicResource ModernButton}" Width="130" Margin="0,0,8,0" Click="OnLoadScript" />
            <Button Content="🔄 Refrescar" Style="{DynamicResource ModernButton}" Width="110" Margin="0,0,8,0" Click="OnReload" />
            <Button Content="🔏 Firmar" Style="{DynamicResource ModernButton}" Width="110" Margin="0,0,8,0" Click="OnSign" />
            <Button Content="✅ Verificar" Style="{DynamicResource ModernButton}" Width="130" Margin="0,0,8,0" Click="OnVerify" />
            <Button Content="🔄 Regenerar" Style="{DynamicResource ModernButton}" Width="140" Click="OnRegenerate" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock x:Name="FilePathText" Foreground="{DynamicResource SecondaryText}" FontSize="12" />
            <TextBlock x:Name="StatusText" Foreground="{DynamicResource WarningBrush}" FontSize="12" Margin="0,4,0,0" />
        </StackPanel>

        <Border Grid.Row="2" Style="{DynamicResource CardBorder}" Padding="0">
            <RichTextBox x:Name="ScriptViewer" Background="{DynamicResource EditorBackground}"
                         Foreground="{DynamicResource PrimaryText}" IsReadOnly="True" BorderThickness="0" />
        </Border>
    </Grid>
</UserControl>
